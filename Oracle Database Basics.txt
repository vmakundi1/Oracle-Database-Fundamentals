--SQL Commands: select, insert, update, delete, create, alter, drop--
--when creating tables, the following are naming constraints--
--1-30 characters, must beging with an alpha character, cannot be an SQL reserved word--
--specify table name, column names, column data types, column sizes, defaults and or constraints--
--alphanumeric characters like such as $, _ and # are restricted--
--CHARACTER DATA TYPES--
--char; max 2000 characters, defaults to a width of 1, fixed-length character data, may specify max width allowed--
--varchar2; max 4,000 characters, variable length character strings--
--INTEGRITY CONSTRAINTS; used to define PK and FK--
--VALUE CONSTRAINTS; restricts values that can be inserted into a column.Such as check CK, not null NN, null N--
--Deleting a table (puts into a recycle bin and can be restored with flashback command)--
--DROP TABLE student--
--Deleting a table completely--
--DROP TABLE student PURGE--
--Deleting a table and all 'referential integrity associated with that table--
--DROP TABLE student CASCADE CONSTRAINTS;--
--To view a table:--
--DESC[ribe] user_tables;--
--select table_name--
--from user_tables;--
--viewing user constraints information--
--DESC[ribe] user_constraints;--
--select constraint_name--
--from user_constraints--
--where table_name='student'--
--ALTERING TABLES: you are prohibited from changing column name--
--however you can add a new column,add a default, change table name, deleting a CK or FK constraint--
--SEQUENCES: a sequential set of numbers that is automatically generated by the DB--
--USED TO CREATE UNIQUE PRIMARY KEYS--
--SYNTAX for  a sequence--
--CREATE SEQUENCE Sequence_Name--
--START WITH x--
--INCREMENT BY y--
--NOCACHE;--
--DROPPING AN EXITING SEQUENCE: DROP SEQUENCE Sequence_Name;--